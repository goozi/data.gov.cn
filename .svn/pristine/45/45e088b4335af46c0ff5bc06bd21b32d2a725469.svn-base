package com.dhccity.base.entity;

import java.util.*;

import org.light.*;

/**
 * <p>Title: BASE_LOG 表对应类</p>
 * <p>Description:BASE_LOG</p>
 * <p>Company: dhccity</p>
 * <p>CreateDate: 2007-01-15 16:55</p>
 * @author liuxd
 * @version 1.0
 */

public class BaseLog extends Ado
{
	private long id; //ID
	private long userId; //用户ID
	private String content; //操作内容
	private String ip; //IP地址
	private Date nodeTime; //记录时间
	private String pcName; //机器名称
	private String meno; //备注
	private String systemName; //系统名称
	private String moduleName; //模块名称
	private String userName; //用户名称

	//{defaultOrder = "o.id";} //默认的排序方式

	public BaseLog()
	{
	}

	public BaseLog(long id)
	{
		this.id = id;
	}

	/**
	 * 对应字段：[ID]  类型：NUMBER(22)
	 */
	public void setId(long id)
	{
		this.id = id;
	}

	/**
	 * 对应字段：[ID]  类型：NUMBER(22)
	 */
	public long getId()
	{
		return this.id;
	}

	/**
	 * 对应字段：[用户ID]  类型：NUMBER(12)
	 */
	public void setUserId(long userId)
	{
		this.userId = userId;
	}

	/**
	 * 对应字段：[用户ID]  类型：NUMBER(12)
	 */
	public long getUserId()
	{
		return this.userId;
	}

	/**
	 * 对应字段：[操作内容]  类型：VARCHAR2(200)
	 */
	public void setContent(String content)
	{
		this.content = content;
	}

	/**
	 * 对应字段：[操作内容]  类型：VARCHAR2(200)
	 */
	public String getContent()
	{
		return this.content == null ? "" : this.content;
	}

	/**
	 * 对应字段：[IP地址]  类型：VARCHAR2(20)
	 */
	public void setIp(String ip)
	{
		this.ip = ip;
	}

	/**
	 * 对应字段：[IP地址]  类型：VARCHAR2(20)
	 */
	public String getIp()
	{
		return this.ip == null ? "" : this.ip;
	}

	/**
	 * 对应字段：[记录时间]  类型：DATE(7)
	 */
	public void setNodeTime(Date nodeTime)
	{
		this.nodeTime = nodeTime;
	}

	/**
	 * 对应字段：[记录时间]  类型：DATE(7)
	 */
	public Date getNodeTime()
	{
		return this.nodeTime;
	}

	/**
	 * 对应字段：[机器名称]  类型：VARCHAR2(50)
	 */
	public void setPcName(String pcName)
	{
		this.pcName = pcName;
	}

	/**
	 * 对应字段：[机器名称]  类型：VARCHAR2(50)
	 */
	public String getPcName()
	{
		return this.pcName == null ? "" : this.pcName;
	}

	/**
	 * 对应字段：[备注]  类型：VARCHAR2(1000)
	 */
	public void setMeno(String meno)
	{
		this.meno = meno;
	}

	/**
	 * 对应字段：[备注]  类型：VARCHAR2(1000)
	 */
	public String getMeno()
	{
		return this.meno == null ? "" : this.meno;
	}

	/**
	 * 对应字段：[系统名称]  类型：VARCHAR2(200)
	 */
	public void setSystemName(String systemName)
	{
		this.systemName = systemName;
	}

	/**
	 * 对应字段：[系统名称]  类型：VARCHAR2(200)
	 */
	public String getSystemName()
	{
		return this.systemName == null ? "" : this.systemName;
	}

	/**
	 * 对应字段：[模块名称]  类型：VARCHAR2(200)
	 */
	public void setModuleName(String moduleName)
	{
		this.moduleName = moduleName;
	}

	/**
	 * 对应字段：[模块名称]  类型：VARCHAR2(200)
	 */
	public String getModuleName()
	{
		return this.moduleName == null ? "" : this.moduleName;
	}

	/**
	 * 对应字段：[用户名称]  类型：VARCHAR2(1000)
	 */
	public void setUserName(String userName)
	{
		this.userName = userName;
	}

	/**
	 * 对应字段：[用户名称]  类型：VARCHAR2(1000)
	 */
	public String getUserName()
	{
		return this.userName == null ? "" : this.userName;
	}

	//===============================以下为自编辑代码===================================

	/**
	 * 查找符合条件的记录集
	 * @param searchField String 搜索字段
	 * @param searchValue String 搜索值
	 * @param startTime Date    开始时间
	 * @param endTime Date      结束时间
	 * @param currPage int      当前页码
	 * @param pageSize int      每页行数
	 * @return List
	 */
	public List search(String searchField, String searchValue, Date startTime, Date endTime, int currPage, int pageSize)
	{
		String sql = "from BaseLog o where o." + searchField + " like '%" + searchValue + "%'";
		if (startTime != null)
		{
			sql += " and o.nodeTime>=" + filterTime(startTime);
		}
		if (endTime != null)
		{
			sql += " and o.nodeTime<=" + filterTime(endTime);
		}
		sql += " order by o.id desc";
		return this.findPage(sql, currPage, pageSize);
	}

}
